import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "FinPath AI — Your Financial Journey Starts with Your Goal",
  description: "FinPath AI helps you organize your financial journey around what you want to achieve. Create your Financial Mission, upload documents, and track your progress.",
  keywords: "financial planning, goal-based finance, fintech, India, financial mission",
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <head>
        <link rel="preconnect" href="https://fonts.googleapis.com" />
        <link rel="preconnect" href="https://fonts.gstatic.com" crossOrigin="anonymous" />
      </head>
      <body className={inter.className}>
        {children}
      </body>
    </html>
  );
}
